# what: The overflow check before each multiply is gone, so 4294967296 wraps to zero and is accepted — atoi's failure, exactly.
#
# Applied by scripts/mutate.sh, which requires the FIND block to match
# exactly once and the named test to fail because of it.
FILE src/text/text.c
TEST Text_Test
--- FIND
static uint8_t textParseDigits ( const uint8_t* const source, uint32_t start,
                                 uint32_t end, uint32_t limit,
                                 uint32_t* magnitude )
{
    uint8_t retVal = TRUE;
    uint32_t i = 0u;
    uint32_t digit = 0u;
    uint32_t accumulated = 0u;

    if ( start >= end )
    {
        retVal = FALSE;
    }
    else
    {
        /* Intentionally blank */
    }

    for ( i = start; ( i < end ) && ( retVal == TRUE ); ++i )
    {
        if ( ( ( uint32_t ) source[ i ] < TEXT_ASCII_ZERO ) ||
             ( ( uint32_t ) source[ i ] > TEXT_ASCII_NINE ) )
        {
            retVal = FALSE;
        }
        else
        {
            digit = ( uint32_t ) source[ i ] - TEXT_ASCII_ZERO;

            if ( accumulated > ( ( limit - digit ) / 10u ) )
            {
                retVal = FALSE;
            }
            else
            {
                accumulated = ( accumulated * 10u ) + digit;
            }
        }
    }

    if ( retVal == TRUE )
    {
        *magnitude = accumulated;
    }
    else
    {
        /* Intentionally blank */
    }

    return ( retVal );
}
--- REPLACE
static uint8_t textParseDigits ( const uint8_t* const source, uint32_t start,
                                 uint32_t end, uint32_t limit,
                                 uint32_t* magnitude )
{
    uint8_t retVal = TRUE;
    uint32_t i = 0u;
    uint32_t digit = 0u;
    uint32_t accumulated = 0u;

    if ( start >= end )
    {
        retVal = FALSE;
    }
    else
    {
        /* Intentionally blank */
    }

    for ( i = start; ( i < end ) && ( retVal == TRUE ); ++i )
    {
        if ( ( ( uint32_t ) source[ i ] < TEXT_ASCII_ZERO ) ||
             ( ( uint32_t ) source[ i ] > TEXT_ASCII_NINE ) )
        {
            retVal = FALSE;
        }
        else
        {
            digit = ( uint32_t ) source[ i ] - TEXT_ASCII_ZERO;

            if ( FALSE == TRUE )
            {
                retVal = FALSE;
            }
            else
            {
                accumulated = ( accumulated * 10u ) + digit;
            }
        }
    }

    if ( retVal == TRUE )
    {
        *magnitude = accumulated;
    }
    else
    {
        /* Intentionally blank */
    }

    return ( retVal );
}
--- END
